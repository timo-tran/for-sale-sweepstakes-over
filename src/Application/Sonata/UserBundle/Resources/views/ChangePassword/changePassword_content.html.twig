{{ form_start(form, {'action': path('sonata_user_change_password')  }) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <button type="submit" class="btn btn-primary">{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}</button>
    </div>
{{ form_end(form) }}
