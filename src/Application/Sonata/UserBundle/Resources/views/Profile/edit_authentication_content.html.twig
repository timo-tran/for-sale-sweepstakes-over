{{ form_start(form, {'action': path('sonata_user_profile_edit_authentication')  }) }}
    {{ form_row(form.email) }}
    {{ form_row(form.current_password) }}
    {{ form_row(form._token) }}
    <div class="hide">
        {{ form_row(form.username) }}
    </div>
    <div class="form-group">
        <button type="submit" name="submit" class="btn btn-primary">{{ 'sonata_user_submit'|trans({}, 'SonataUserBundle') }}</button>
    </div>
{{ form_end(form, {'render_rest': false}) }}
